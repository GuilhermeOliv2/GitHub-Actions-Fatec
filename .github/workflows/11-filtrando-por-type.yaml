name: 11 - Filtrando por Type

on:
  pull_request:
    types: [opened, reopened, closed]

jobs:
  test-type:
    runs-on: ubuntu-latest
    steps:
      - name: Mensagem de execução
        run: echo "Workflow disparado por TYPE do pull_request!"
